# Services

Future backend service or worker boundaries may live here if the product later needs more than one deployable component.

Current rule:
- keep this directory documentation-only until implementation begins
